# نظام اختبار الأداء (NasGo Performance Tester)

هذا النظام مصمم لاختبار أداء موقع NasGo بشكل شامل واحترافي باستخدام Google Lighthouse.

## المميزات
- 🚀 **اكتشاف تلقائي للصفحات:** يقرأ مجلد `app` تلقائياً.
- 📱 **اختبار متعدد:** يختبر كل صفحة على Desktop و Mobile بشكل منفصل.
- 📊 **تقرير عربي مفصل:** يولد ملف `performance-report.md` يحتوي على تحليل كامل وتوصيات.
- 📉 **تحليل الموارد:** يحلل أحجام الصور والملفات غير المحسنة.

## المتطلبات
- Node.js
- Google Chrome مثبت على الجهاز.

## التثبيت
```bash
npm install
```

## الاستخدام

### 1. تشغيل الموقع (مهم جداً!)
يجب أن يكون الموقع يعمل أولاً (يفضل Production Build لنتائج دقيقة).

```bash
# في ترمينال منفصل:
npm run build
npm run start
```

### 2. تشغيل الاختبار
```bash
npm run test:performance
```

أو لتحديد رابط معين (مثلاً لو السيرفر على بورت مختلف):
```bash
npm run test:performance -- --url=http://localhost:3000
```

## المخرجات
- **التقرير النهائي:** `performance-report.md` (في المجلد الرئيسي).
- **التقارير الخام:** مجلد `lighthouse-reports/` يحتوي على ملفات JSON لكل اختبار.
